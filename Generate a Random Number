# Program to generate a random number between a given range

# importing the random module
import random

a = int(input('Enter a start number: '))
b = int(input('Enter a final number: '))

print(random.randint(a,b))
